package clustering;
/*
import static org.junit.Assert.*;
import graph.UndirectedGraph;
import graph.WeightedEdge;

import org.junit.After;
import org.junit.Before;
import org.junit.Test;

public class ClusteringSuite {

	@Before
	public void setUp() throws Exception {
	}

	@After
	public void tearDown() throws Exception {
	}

	@Test
	public void testBaseClustering() {
		UndirectedGraph g = new UndirectedGraph();
		
		g.addEdge(1, 2, 10);
		g.addEdge(2, 3, 10);
		g.addEdge(3, 4, 10);
		g.addEdge(4, 1, 10);
		
		Clustering clust = new Clustering(g, 4);
		
		assertEquals("test base case", 10.0, clust.spacing(), 0.0001);
	}

	
	@Test 
	public void testMoreSophisticatedClustering() {
		UndirectedGraph g = new UndirectedGraph();
		
		g.addEdge(1, 2, 10);
		g.addEdge(2, 5, 1);
		g.addEdge(2, 3, 10);
		g.addEdge(2, 6, 1);
		g.addEdge(2, 7, 1);
		g.addEdge(2, 8, 1);
		g.addEdge(2, 9, 1);
		g.addEdge(2, 3, 10);
		g.addEdge(3, 4, 10);
		g.addEdge(4, 1, 10);
		
		Clustering clust = new Clustering(g, 4);
		
		assertEquals("test base case", 10.0, clust.spacing(), 0.0001);
	}
}
*/